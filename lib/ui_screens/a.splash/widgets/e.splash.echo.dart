part of '../_index.dart';

class SplashEcho extends StatelessWidget {
  const SplashEcho({Key? key}) : super(key: key);

  SplashCtrl get ct => Ctrl.splash;
  SplashData get dt => Data.splash.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
