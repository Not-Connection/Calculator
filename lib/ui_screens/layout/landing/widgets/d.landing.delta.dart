part of '../_index.dart';

class LandingDelta extends StatelessWidget {
  const LandingDelta({Key? key}) : super(key: key);

  LandingCtrl get ct => Ctrl.landing;
  LandingData get dt => Data.landing.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
